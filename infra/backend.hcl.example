# Copy to backend.hcl (gitignored) and fill in the bucket from `bootstrap` output.
#   terraform init -backend-config=backend.hcl
bucket = "best-online-shop-tfstate-176971015975"
key    = "prod/terraform.tfstate"
region = "eu-central-1"
