{
  "name": "@shop/email",
  "version": "0.1.0",
  "private": true,
  "description": "Transactional email — transports (SES / console / stub) and templates. Pure transport + rendering; tokens and DB rows belong to the API layer.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build:lambda": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'add eslint when shared config lands'"
  },
  "dependencies": {
    "@aws-sdk/client-sesv2": "^3.1066.0",
    "@aws-sdk/client-sqs": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
